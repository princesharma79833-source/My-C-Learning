#include <stdio.h>

int main()
{
    int i = 0;

    while (i < 20)
    {
        printf("the value of i is %d\n", i);
        i++;                                       // i++ is same as i=i+1
    }
    return 0;
}