#include <stdio.h>

int main()
{
    // char str[20]="PRINCE";
    char *ptr = "PRINCE"; // This tells the compiler to store the string in memory and assigned address is stored in achar pointer.
    /*1. Once a string is defined using char st [] = “harry”, it cannot be reinitialized to
something else.
2. A string defined using pointers can be reinitialized.

    printf("%u", ptr);*/

    return 0;
}