#include <stdio.h>
#include <math.h>                 // math.h library is mathematical functions library

int main() {
    float a=9;                                               
    printf("the area of the square is %.3f\n", pow(a,2)); // pow function is used to calculate power of a number , here pow(a,2) means a^2
    
    
    return 0;
}